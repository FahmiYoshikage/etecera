# Starship Configuration - Beautiful Prompt with Fallback Characters
# https://starship.rs/config/

format = """
$os\
$container\
$username\
$hostname\
$directory\
$git_branch\
$git_status\
$package\
$nodejs\
$python\
$rust\
$golang\
$java\
$php\
$lua\
$docker_context\
$cmd_duration\
$line_break\
$character"""

add_newline = true

# OS Icon
[os]
disabled = false
style = "bold blue"
format = "[$symbol]($style) "

[os.symbols]
Fedora = "🎩"
Ubuntu = "🟠"
Debian = "🔴"
Arch = "🔷"
Alpine = "🏔️"
CentOS = "💜"
Linux = "🐧"
Macos = "🍎"
Windows = "🪟"

# Container (distrobox/toolbox)
[container]
symbol = "📦"
style = "bold red"
format = '[$symbol $name]($style) '

# Username
[username]
style_user = "bold green"
style_root = "bold red"
format = "[$user]($style)@"
show_always = true

# Hostname
[hostname]
ssh_only = false
format = "[$hostname]($style) "
style = "bold yellow"

# Directory
[directory]
style = "bold cyan"
truncation_length = 4
truncate_to_repo = true
format = "in [$path]($style) "
home_symbol = "🏠"
read_only = "🔒"

# Git Branch
[git_branch]
symbol = "🌿"
style = "bold purple"
format = "on [$symbol $branch]($style) "

# Git Status
[git_status]
format = '([$all_status$ahead_behind]($style) )'
style = "bold red"
conflicted = "⚔️"
ahead = "⬆${count}"
behind = "⬇${count}"
diverged = "↕${ahead_count}↓${behind_count}"
untracked = "❓"
stashed = "📦"
modified = "✏️"
staged = "✅"
renamed = "📝"
deleted = "🗑️"

# Package
[package]
symbol = "📦"
format = "pkg [$symbol$version]($style) "
style = "bold 208"

# Node.js
[nodejs]
symbol = "⬢"
format = "via [$symbol node $version]($style) "
style = "bold green"

# Python
[python]
symbol = "🐍"
format = "via [$symbol python $version]($style) "
style = "bold yellow"

# Rust
[rust]
symbol = "🦀"
format = "via [$symbol rust $version]($style) "
style = "bold red"

# Go
[golang]
symbol = "🐹"
format = "via [$symbol go $version]($style) "
style = "bold cyan"

# Java
[java]
symbol = "☕"
format = "via [$symbol java $version]($style) "
style = "bold red"

# PHP
[php]
symbol = "🐘"
format = "via [$symbol php $version]($style) "
style = "bold 147"

# Lua
[lua]
symbol = "🌙"
format = "via [$symbol lua $version]($style) "
style = "bold blue"

# Docker
[docker_context]
symbol = "🐳"
format = "via [$symbol docker $context]($style) "
style = "bold blue"

# Command duration
[cmd_duration]
min_time = 2000
format = "⏱️ [$duration]($style) "
style = "bold yellow"

# Prompt character - colorful arrow
[character]
success_symbol = "[➜](bold green)"
error_symbol = "[✗](bold red)"
vimcmd_symbol = "[V](bold purple)"
